apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: conduit-frontend
  ports:
    - port: 80
      targetPort: 80
  type: NodePort   # exposes frontend externally
