# .github/workflows/build-push.yml
name: build-and-push
on:
  push:
    branches: [ feature-automation-workflow ]
    paths:
      - 'backend'        # submodule pointer change
      - 'frontend'       # submodule pointer change
      - 'docker-compose.yml'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true    # checkout submodules at the pinned commits


      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Compute OWNER (lowercase; prefer secret GHCR_USERNAME, else repo owner)
        run: |
          OWNER="${{ secrets.GHCR_USERNAME }}"
          if [ -z "$OWNER" ]; then OWNER="${{ github.repository_owner }}"; fi
          echo "OWNER=$(echo "$OWNER" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build + push backend (from submodule ./backend)
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/conduit-backend:${{ github.ref_name }}-${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/conduit-backend:${{ github.ref_name }}

      - name: Build + push frontend (from submodule ./frontend)
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/conduit-frontend:${{ github.ref_name }}-${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.OWNER }}/conduit-frontend:${{ github.ref_name }}
